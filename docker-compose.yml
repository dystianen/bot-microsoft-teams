services:
  teams-bot:
    build: .
    shm_size: '2gb'
    container_name: microsoft-teams-bot
    restart: always
    network_mode: host
    env_file: .env
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'
